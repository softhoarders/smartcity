# Static Images

This directory stores image assets used by the server UI.

Current asset:

- `traffic_bg.png`: traffic background image for visual presentation in the web interface.

Keep image filenames stable when templates or CSS reference them directly.

